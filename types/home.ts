export type ServiceItem = {
  title: string;
  description: string;
};

export type GalleryItem = {
  name: string;
  category: string;
  image: string;
};

export type TestimonialItem = {
  quote: string;
  author: string;
  role: string;
};

export type StatItem = {
  value: string;
  label: string;
};
